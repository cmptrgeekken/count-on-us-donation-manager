<div
  class="count-on-us-widget count-on-us-widget--directory"
  data-count-on-us-causes-directory
  data-proxy-base="/apps/count-on-us"
  data-layout="{{ block.settings.layout | escape }}"
  data-show-links="{{ block.settings.show_links | json }}"
  data-show-badges="{{ block.settings.show_badges | json }}"
>
  <div class="count-on-us-widget__header">
    <div class="count-on-us-widget__heading" role="heading" aria-level="2">{{ block.settings.heading | escape }}</div>
    {% if block.settings.description != blank %}
      <div class="count-on-us-widget__description">{{ block.settings.description | escape }}</div>
    {% endif %}
  </div>
  <div class="count-on-us-widget__panel" data-count-on-us-directory-panel>
    <div class="count-on-us-widget__status">Loading causes...</div>
  </div>
</div>

{% schema %}
{
  "name": "Causes directory",
  "target": "section",
  "enabled_on": {
    "templates": ["page"]
  },
  "stylesheet": "donation-widget.css",
  "javascript": "public-directory.js",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Causes we support"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Learn about the causes connected to our donation-enabled products."
    },
    {
      "type": "select",
      "id": "layout",
      "label": "Layout",
      "default": "cards",
      "options": [
        { "value": "cards", "label": "Cards" },
        { "value": "list", "label": "List" }
      ]
    },
    {
      "type": "checkbox",
      "id": "show_links",
      "label": "Show public links",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_badges",
      "label": "Show nonprofit badges",
      "default": true
    }
  ]
}
{% endschema %}
